from service.models import models


def save_comments(comments: list[models.Comment]):
    # TODO: For the sake of the technical interview we will create
    # this dummy function so we are able to mock the return.
    pass
